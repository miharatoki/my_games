<% if current_user.following?(user) %>
  <%= link_to (t '.unfollow'), user_show_unfollow_path(user.id), remote: true %>
<% else %>
  <%= link_to (t '.follow'), user_show_follow_path(user.id), remote: true %>
<% end %>